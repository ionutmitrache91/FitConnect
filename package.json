{
  "name": "fitconnect",
  "version": "1.0.0",
  "private": true,
  "description": "FitConnect Agile MVP with React, Express, SQLite, JWT authentication, events, RSVP, and notifications.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "test": "npm test --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "keywords": [
    "fitness",
    "meetups",
    "react",
    "express",
    "sqlite"
  ],
  "author": "",
  "license": "MIT"
}

